[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx nest build"]

[start]
cmd = "npx prisma db push --skip-generate && node dist/main.js"
